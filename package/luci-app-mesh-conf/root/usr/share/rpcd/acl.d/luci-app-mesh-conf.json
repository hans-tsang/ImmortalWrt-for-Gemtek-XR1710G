{
	"luci-app-mesh-conf": {
		"description": "Grant access to the mesh configuration page",
		"read": {
			"ubus": {
				"luci.meshconf": [ "getStatus", "scanPeers" ]
			}
		},
		"write": {
			"ubus": {
				"luci.meshconf": [ "applyMesh", "applyRoaming", "applySync", "syncPeer" ]
			}
		}
	}
}
